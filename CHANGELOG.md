# Changelog

本文件记录 `astrbot_plugin_tts_emotion_tags` 各版本的更新内容。

## [0.3.0] - 2026-08-23

### 新增
- **正则兜底注入**：当 LLM 没有主动输出表现标签时，插件会根据场景关键词兜底注入一个最贴切的表情（如"好耶"→`(chuckle)`、"呜呜"→`(sighs)`、"诶？"→`(inhale)`、"陪你"→`(breath)`）。
- 新增配置项 `insert_probability`（兜底注入概率，默认 0.38）。
- 新增配置项 `max_tags_per_message`（每段最多标签数，默认 1）。

### 改进
- 采用"LLM 语义 + 正则兜底"双保险：LLM 能理解就用 LLM，LLM 忘了就用正则兜底，克制不喧宾夺主。
- README 补充"正则兜底"说明与"反馈渠道"。

## [0.2.0] - 2026-08-22

### 新增
- 接管式 TTS 合成：插件自己调用 MiniMax 生成带表演标签的语音，支持"开插件走插件、关插件走内置 TTS"的开关语义。
- 强制规范化标签（白名单过滤、别名归一化、非法丢弃）。
- QQ 文本标签剥离（保证发送给用户的文字永远干净）。

### 修复
- 修复分段回复产生多条语音的问题（合并为一条语音 + 一条干净文本）。
- 修复换行被压缩的问题（保留换行结构）。
- 修复开启插件后每条回复都变语音且无文本的问题（引入 `trigger_probability` 触发概率，默认 0.3，并始终保留文本）。

## [0.1.0] - 2026-08-22

### 初始版本
- 基础功能：LLM 回复注入 MiniMax 表现标签（`(chuckle)`、`(sighs)` 等），剥离 QQ 文本标记。
